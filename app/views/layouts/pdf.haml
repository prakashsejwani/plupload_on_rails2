%html
  %head
    %style{:type=>"text/css"}
      - # inline the required css files
      - ['scaffold', 'style'].each do |stylesheet|
        = File.read "#{Rails.public_path}/stylesheets/#{stylesheet}.css"

  %body
    = yield
